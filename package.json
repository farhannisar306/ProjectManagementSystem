{
    "name": "project-management-system",
    "private": true,
    "scripts": {
        "all": "concurrently \"npm run continuous --prefix backend\" \"npm run dev --prefix frontend\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "dependencies": {
        "@types/path-to-regexp": "^1.0.29",
        "path-to-regexp": "^8.2.0"
    }
}
